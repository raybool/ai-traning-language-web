# CoursesPageContent

## Описание файлов внутри папки компонента

- `index.ts` — файл экспортирует только те сущности, которые должны использоваться за пределами папки компонента.
- `CoursesPageContent.tsx` — основной файл компонента.
- `CoursesPageContent.module.scss` — файл стилей компонента.
