NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
NEXT_PUBLIC_WEBAPP_NAME=AI Trainer
# Comma-separated list of allowed external dev origins (scheme://host[:port])
NEXT_ALLOWED_DEV_ORIGINS=
# Enable browser fallback auth for local development without Telegram WebApp SDK
NEXT_PUBLIC_WEBAPP_DEV_AUTH=false
